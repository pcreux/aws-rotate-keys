require "spec_helper"

describe Aws::Rotate::Keys do
  it "has a version number" do
    expect(Aws::Rotate::Keys::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
